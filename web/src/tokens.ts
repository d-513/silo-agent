// Hex copies of the DESIGN.md palette for renderers that paint to a canvas
// (xterm) and cannot resolve CSS custom properties. Keep in sync with the
// @theme block in index.css.
export const palette = {
  hatch: "#141413",
  matte: "#0e0e0d",
  cobalt: "#2b4fc7",
  white: "#ffffff",
  canvas: "#faf9f7",
} as const;
