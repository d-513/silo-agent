// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: silo/v1/mcpbridge.proto

package silov1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	http "net/http"
	v1 "silo.agent/gen/silo/v1"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// MCPHostName is the fully-qualified name of the MCPHost service.
	MCPHostName = "silo.v1.MCPHost"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// MCPHostTunnelProcedure is the fully-qualified name of the MCPHost's Tunnel RPC.
	MCPHostTunnelProcedure = "/silo.v1.MCPHost/Tunnel"
)

// MCPHostClient is a client for the silo.v1.MCPHost service.
type MCPHostClient interface {
	Tunnel(context.Context) *connect.BidiStreamForClient[v1.BridgeFrame, v1.BridgeFrame]
}

// NewMCPHostClient constructs a client for the silo.v1.MCPHost service. By default, it uses the
// Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewMCPHostClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) MCPHostClient {
	baseURL = strings.TrimRight(baseURL, "/")
	mCPHostMethods := v1.File_silo_v1_mcpbridge_proto.Services().ByName("MCPHost").Methods()
	return &mCPHostClient{
		tunnel: connect.NewClient[v1.BridgeFrame, v1.BridgeFrame](
			httpClient,
			baseURL+MCPHostTunnelProcedure,
			connect.WithSchema(mCPHostMethods.ByName("Tunnel")),
			connect.WithClientOptions(opts...),
		),
	}
}

// mCPHostClient implements MCPHostClient.
type mCPHostClient struct {
	tunnel *connect.Client[v1.BridgeFrame, v1.BridgeFrame]
}

// Tunnel calls silo.v1.MCPHost.Tunnel.
func (c *mCPHostClient) Tunnel(ctx context.Context) *connect.BidiStreamForClient[v1.BridgeFrame, v1.BridgeFrame] {
	return c.tunnel.CallBidiStream(ctx)
}

// MCPHostHandler is an implementation of the silo.v1.MCPHost service.
type MCPHostHandler interface {
	Tunnel(context.Context, *connect.BidiStream[v1.BridgeFrame, v1.BridgeFrame]) error
}

// NewMCPHostHandler builds an HTTP handler from the service implementation. It returns the path on
// which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewMCPHostHandler(svc MCPHostHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	mCPHostMethods := v1.File_silo_v1_mcpbridge_proto.Services().ByName("MCPHost").Methods()
	mCPHostTunnelHandler := connect.NewBidiStreamHandler(
		MCPHostTunnelProcedure,
		svc.Tunnel,
		connect.WithSchema(mCPHostMethods.ByName("Tunnel")),
		connect.WithHandlerOptions(opts...),
	)
	return "/silo.v1.MCPHost/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case MCPHostTunnelProcedure:
			mCPHostTunnelHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedMCPHostHandler returns CodeUnimplemented from all methods.
type UnimplementedMCPHostHandler struct{}

func (UnimplementedMCPHostHandler) Tunnel(context.Context, *connect.BidiStream[v1.BridgeFrame, v1.BridgeFrame]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("silo.v1.MCPHost.Tunnel is not implemented"))
}
